# Kerr 黑洞物理研究备忘

> 本文件汇总本项目可视化所依赖的相对论物理与关键文献，作为着色器/粒子积分器的规格来源。

## 1. Kerr 度量（Boyer–Lindquist，几何单位 G = c = 1）

质量 M，自旋参数 a = J/M，|a| ≤ M。记

- Σ = r² + a² cos²θ
- Δ = r² − 2Mr + a²

线元：

ds² = −(1 − 2Mr/Σ) dt² − (4Mar sin²θ/Σ) dt dφ + (Σ/Δ) dr² + Σ dθ² + (r² + a² + 2Ma² r sin²θ/Σ) sin²θ dφ²

关键半径：

| 名称 | 公式 (M=1) |
|------|------------|
| 外视界 r₊ | 1 + √(1 − a²) |
| 能层（赤道） | 2 |
| 光子球（顺行赤道） | 2[1 + cos((2/3) arccos(−a))] |
| ISCO（顺行） | 3 + Z₂ − √((3−Z₁)(3+Z₁+2Z₂))，Z₁、Z₂ 见下 |

Z₁ = 1 + (1−a²)^{1/3}[(1+a)^{1/3} + (1−a)^{1/3}]
Z₂ = √(3a² + Z₁²)

赤道圆轨道角速度（精确）：Ω = 1 / (r^{3/2} + a)（顺行，M=1）。

参考拖曳（ZAMO 角速度）：

ω = −g_{tφ} / g_{φφ} = 2Mar / [(r²+a²)² − Δ a² sin²θ]

## 2. 零测地线（光线）

Hamilton–Jacobi 可分离（Carter）。守恒量：能量 E、轴向角动量 L、Carter 常数 Q。

对光子（μ = 0）：

- P(r) = E(r² + a²) − aL
- R(r) = P² − Δ[(L − aE)² + Q]
- Θ(θ) = Q − L² cot²θ + a² E² cos²θ

一阶形式：

- Σ dr/dλ = ±√R
- Σ dθ/dλ = ±√Θ
- Σ dφ/dλ = −(aE − L/sin²θ) + aP/Δ
- Σ dt/dλ = −a(aE sin²θ − L) + (r²+a²) P/Δ

实现策略：着色器内对每个像素**反向积分**零测地线（Heun/RK2），从相机 ZAMO 标架构造 (E, L, Q) 与初始 (ṙ, θ̇, φ̇) 符号，击中（倾斜）赤道吸积盘则按 g 因子累加发射，落入视界则为阴影，否则采样透镜后星场。**光子环与高阶次级像由测地线映射自然产生，不加人工光晕。**

### 实现保真度（与理想物理对照）

| 项目 | 状态 |
|------|------|
| Kerr 度量 / Carter 分离 | ✅ R(r)、Θ(θ)、φ̇ 完整一阶形式 |
| 初值 (E,L,Q) | ✅ ZAMO 正交标架 |
| 数值积分 | ✅ Heun，自适应步长；步数可调 |
| 光子环 | ✅ 透镜放大导致的亮环（非合成 glow） |
| 阴影 | ✅ r ≤ r₊ 判定捕获（自旋不对称） |
| 多普勒 + 引力红移 | ✅ g = E/(−k_μ u^μ)，I_obs = g³ I_em |
| 盘轨道 4-速度 | ✅ 赤道 Kerr 圆轨道 u^μ，Ω = 1/(r^{3/2}+a) |
| 盘结构 | ⚠ 薄盘 + 经验湍流纹理（非 GRMHD） |
| 吸入物质粒子 | ⚠ 开普勒 + 黏滞漂移近似（非测地线流体） |
| 高阶像光深 | ⚠ 薄盘可多次穿越累加；不严格等价不透明盘 |

## 3. 成像物理

1. **引力透镜**：光线被弯曲，盘的背面在视界上方形成次级像（Interstellar / Luminet 外观）。
2. **光子环**：接近光子球的光线绕多圈，形成细亮环。
3. **阴影**：捕获光线形成非对称阴影（自旋导致不对称）。
4. **多普勒集束 + 引力红移**：观测频率比 **g = E / (−k_μ u^μ_em)**（E 为光子在无穷远能量，u^μ 为发射元四维速度）；相对论集束 **I_obs = g³ I_em**。接近侧偏亮偏蓝，远离侧偏暗偏红。**不是**逐步累加的光晕。
5. **参考拖曳**：光子路径被扭转；盘的内缘随黑洞旋转“拖着走”。

## 4. 物质吸入

- 赤道盘粒子：Ω(r) = 1/(r^{3/2} + a)，向外的黏滞损失 → 缓慢向内漂移。
- r > r_ISCO：近圆开普勒（相对论）轨道；r ≤ r_ISCO：向视界的自由下落（plunge）。
- 接近视界：周向拖曳增强，轨迹被拧进自旋方向；越过 r₊ 后淡出。

## 5. 关键文献

| 文献 | 用途 |
|------|------|
| Kerr, R. P. (1963), PRL 11, 237 | Kerr 解原始文献 |
| Kerr, arXiv:0706.1109 | Kerr/Kerr–Schild 发现史与物理解释 |
| Carter, B. (1968), PR 174, 1559 | Hamilton–Jaci 分离与 Carter 常数 |
| Bardeen, Press & Teukolsky (1972), ApJ 178, 347 | 旋转黑洞局部非转动系 (ZAMO) |
| Luminet (1979), A&A 75, 228 | 薄吸积盘成像（次级像） |
| Bozza, arXiv:0911.2187 | 黑洞引力透镜综述（高阶像、自旋） |
| Fabian & Miniutti, arXiv:astro-ph/0507409 | Kerr 吸积盘 X 射线/铁线（红移、集束） |
| James, von Tunzelmann, Franklin & Thorne, arXiv:1502.03808 | Interstellar 黑洞光线反向追踪方法 |
| James et al., arXiv:1502.03809 | 相机标架 + 测地线反向映射（实现参考） |
| Glampedakis & Pappas, arXiv:1806.09333 | 球形光子轨道 / 陷光轨道 |
| Hawley & De Villiers, arXiv:astro-ph/0402665 | GRMHD 吸积盘综述（盘结构直觉） |

## 6. 数值与实现约定

- 工作单位：M = 1；默认自旋 a = 0.9。
- 坐标：大距离处与欧氏球坐标近似；积分在 (r, θ, φ) 上进行。
- 相机自由飞行：用 ZAMO 标架把屏幕射线变为局部光子波矢。
- 性能：步数可调（64–256 RK4）；远处用大步长、近处用小步长。
